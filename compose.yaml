services:
  geons:
    image: ghcr.io/viktor45/geons:latest
    container_name: geons
    restart: unless-stopped
    ports:
      - "5300:5300/udp"
    environment:
      - GEONS_CONFIG=/data/config.yaml
    volumes:
      - ./data:/data:ro
